# Environment notes (from current machine)
# Python: 3.11.9
# NVIDIA Driver: >=570.65 required for CUDA 12.8 runtime
# CUDA Driver Runtime (nvidia-smi): >=12.8 after driver update
# PyTorch build: 2.7.1+cu128 (CUDA 12.8)
# torch.cuda.is_available(): True
# GPU: NVIDIA GeForce RTX 3050 Laptop GPU

# Use the PyTorch cu128 wheel index for +cu128 builds
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.7.1+cu128
torchvision==0.22.1+cu128
torchaudio==2.7.1+cu128

einops==0.8.2
transformers>=4.40.0
ninja==1.13.0
numpy>=1.24.0
pandas==2.3.3
scikit-learn==1.8.0
pytest==9.0.2
tqdm==4.67.3
streamlit==1.55.0
PyYAML>=6.0
click>=8.0
retry>=0.9.2
Pillow>=10.0.0
matplotlib>=3.8.0
joblib>=1.3.0
tensorboard>=2.14.0
packaging>=23.0

# Installed package name is mamba_ssm (distribution mamba-ssm)
#mamba-ssm==2.3.1

